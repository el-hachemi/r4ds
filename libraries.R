# Loading libriries

library(tidyverse) 
library(nycflights13) # Datasets
library(Lahman) # Datasets

library(modelr) # For the models section
options(na.action = na.warn) # For warning when dropping missing values

library(lubridate) # for dealing with date and time objects
